# Color Picker

This is a console application that creates a GUI window.
The GUI window includes a button that prompts the user to enter three numbers in the console. This will set the color code in RGB and fill the window with that color.

The console application is a placeholder for the user interface. The program is planned to be interacted exclusively via the GUI window.